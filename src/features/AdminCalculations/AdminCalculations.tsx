import React from 'react';
import { Helmet } from 'react-helmet';

const AdminCalculations = () => (
  <div>
    <Helmet>
      <title>Admin Calculations | Bunker</title>
    </Helmet>
    <div>Admin Calculations</div>
  </div>
);

export default AdminCalculations;
